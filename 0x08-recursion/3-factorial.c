#include "main.h"

/**
 * factorial - finds factorial
 * @n: int
 * R);
}
else if (n int factorial(ineturn: int
 */

t n)
{

if (n < 0)
{
	return (-1== 0)
{
	return (1);
}

return (n * factorial(n - 1));

}
